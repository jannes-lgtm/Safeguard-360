[build]
  command   = "npm run build"
  publish   = "dist"
  functions = "api"

# Route all requests through the React app (required for React Router)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
